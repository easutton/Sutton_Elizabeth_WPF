//Calculate how old Sparky the pit bull is in dog years based on his actual age.
var sparkysAge = 2;
const dogYears = 7;

//multiply sparkysAge and dogYears
var actualAge = sparkysAge * dogYears;

//print out Sparky's age in dog years
console.log("Sparky is " + sparkysAge + " human years old which is " + dogYears + " in dog years.");


//Calculate the number of slices each person gets
const partyPeople = 25;
const pizzaSlices = 8;
const pizzaOrdered = 12;

//multiply pizzaSlices and pizzaOrdered, then divide by partyPeople
var slicesPerPerson = (pizzaSlices * pizzaOrdered) / partyPeople;

//print out the number of slices each person gets
console.log("Each person ate " + slicesPerPerson + " slices of pizza at the party.")